import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#668068",
    },
    secondary: {
      main: "#f1e9e7",
    },
    tertiary: {
      main: "#eaf0e9",
    },
    background: {
      default: "#fff",
    },
  },
});

export default theme;
